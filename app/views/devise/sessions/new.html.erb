<%= render layout: "devise/shared/auth_card",
           locals: { title: "Welcome back", subtitle: "Log in to pick up where your channels left off." } do %>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%# Checked by default: without the remember cookie the session lasts until the
          browser closes, which is what made signing in a weekly event. Still a checkbox,
          so it can be turned off on a machine that is not yours. %>
      <%= f.input :remember_me, as: :boolean, input_html: { checked: true } if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in", class: "auth-submit" %>
    </div>
  <% end %>
<% end %>
